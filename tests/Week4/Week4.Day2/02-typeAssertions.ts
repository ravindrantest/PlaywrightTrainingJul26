
let x : string = "username"

// as or <> for typeAssertion

/* when the username comes from outside source like .env where we manage all our credentials */

/* await page.locator("#username").fill(username as string )  //  username should be string
await page.locator("#password").fill(pwd)*/